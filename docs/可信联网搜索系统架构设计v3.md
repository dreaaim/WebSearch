# 可信联网搜索系统架构设计 v3

## 文档信息

- **版本**: v3
- **更新日期**: 2026-04-25
- **状态**: 正式版

---

## 目录

1. [v2 问题回顾与 v3 改进概述](#1-v2-问题回顾与-v3-改进概述)
2. [混合过滤引擎 (HybridFilterEngine)](#2-混合过滤引擎-hybridfilterengine)
3. [内容获取器 (ContentFetcher)](#3-内容获取器-contentfetcher)
4. [可信度天梯机制 (TrustRankLadder)](#4-可信度天梯机制-trustrankladder)
5. [多因子 Reranker](#5-多因子-reranker)
6. [事实提取器 (FactExtractor)](#6-事实提取器-factextractor)
7. [事实桶聚类 (FactBucketCluster)](#7-事实桶聚类-factbucketcluster)
8. [正交碰撞检测器 (OrthogonalCollisionDetector)](#8-正交碰撞检测器-orthogonalcollisiondetector)
9. [SearchOrchestrator v3 完整流程](#9-searchorchestrator-v3-完整流程)
10. [v2 到 v3 迁移指南](#10-v2-到-v3-迁移指南)
11. [附录 A: v3 模块列表](#附录-a-v3-模块列表)

---

## 1. v2 问题回顾与 v3 改进概述

### 1.1 v2 架构局限性

v2 架构在生产环境中暴露了以下核心问题：

| 问题类别 | 具体表现 | 影响程度 |
|---------|---------|---------|
| **LLM 性能瓶颈** | 每次搜索都需要调用 LLM 进行初筛，成本高、延迟大 | 高 |
| **静态黑白名单** | 信源可信度固定不变，无法动态适应新信源 | 高 |
| **语义相似度局限** | 仅基于 Embedding 相似度检测碰撞，缺乏结构化事实理解 | 中 |
| **缺乏时序考量** | 未考虑信源的长期可信度积累 | 中 |
| **全文信任传递** | 整篇文档可信度统一，未区分事实层级 | 中 |

### 1.2 v3 核心改进

v3 架构针对上述问题进行了系统性改进：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              v3 核心改进                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│  1. 混合过滤引擎 (HybridFilterEngine)                                        │
│     → 用 Embedding + BM25 高效初筛，替代 LLM 调用                           │
│                                                                             │
│  2. 可信度天梯机制 (TrustRankLadder)                                         │
│     → ELO 动态评分，新信源可积累可信度，老信源可降级                           │
│                                                                             │
│  3. 多因子 Reranker                                                         │
│     → 时效性 + 相关性 + 可信度 三维加权排序                                   │
│                                                                             │
│  4. 结构化事实分析                                                           │
│     → NLI/SPO/数值/时间 多维度事实提取与碰撞检测                              │
│                                                                             │
│  5. 正交碰撞检测                                                             │
│     → 多维度独立碰撞检测，碰撞系数阈值触发 LLM 复核                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 v3 架构总览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              SearchAgent (LLM)                              │
│                       自然语言查询 → 可信事实清单                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         SearchOrchestrator (v3)                             │
│                            搜索编排器 - 核心调度中心                            │
└─────────────────────────────────────────────────────────────────────────────┘
         │              │              │              │
         ▼              ▼              ▼              ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│  QueryRewriter  │ │HybridFilterEngine│ │  ContentFetcher │ │MultiReranker    │
│    查询改写器    │ │   混合过滤引擎    │ │   内容获取器     │ │   多因子重排    │
├─────────────────┤ ├─────────────────┤ ├─────────────────┤ ├─────────────────┤
│ • 意图理解      │ │ • Embedding    │ │ • URL 访问      │ │ • 时效性得分   │
│ • 查询发散      │ │   相似度        │ │ • 文本提取      │ │ • 相关性得分   │
│ • 查询增强      │ │ • BM25 词频     │ │ • 异常处理      │ │ • 天梯得分     │
│ • 语法转换      │ │ • 混合评价      │ │                 │ │ • 加权排序     │
└─────────────────┘ └─────────────────┘ └─────────────────┘ └─────────────────┘
                                                                  │
         ┌─────────────────────────────────────────────────────────┘
         ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            LLMRefiner (LLM 精筛)                            │
│                       Title/Content/URL 二次精筛                             │
│  ┌─────────────────┐ ┌─────────────────┐ ┌───────────────────────────────┐  │
│  │  内容相关性判断   │ │  内容质量评估    │ │       事实密度评估            │  │
│  │  (Relevance)    │ │  (Quality)      │ │   (Fact Density)            │  │
│  └─────────────────┘ └─────────────────┘ └───────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Fact Processing Pipeline                          │
│                            事实处理流水线                                     │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────────────────┐  │
│  │  FactExtractor  │ │FactBucketCluster│ │OrthogonalCollisionDetector │  │
│  │   事实提取器      │ │   事实桶聚类     │ │     正交碰撞检测器            │  │
│  ├─────────────────┤ ├─────────────────┤ ├─────────────────────────────┤  │
│  │ • LLM提示词提取 │ │ • Embedding    │ │ • NLI 冲突检测              │  │
│  │ • NLI 分析      │ │   相似度计算    │ │ • SPO 冲突检测              │  │
│  │ • SPO 三元组    │ │ • 事实桶划分    │ │ • 数值/时间冲突检测          │  │
│  │ • 数值/时间提取 │ │                 │ │ • 碰撞系数计算               │  │
│  │ • 可信度评分    │ │                 │ │ • LLM 复核（高碰撞时）       │  │
│  └─────────────────┘ └─────────────────┘ └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         TrustRankLadder (可信度天梯)                         │
│                            ELO 动态评分机制                                   │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────────────────┐  │
│  │  信源注册       │ │  分数查询       │ │       ELO 更新               │  │
│  │  新信源初始1000 │ │  实时查询可信度  │ │  事实验证后调整信源分数        │  │
│  └─────────────────┘ └─────────────────┘ └─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 混合过滤引擎 (HybridFilterEngine)

### 2.1 模块概述

HybridFilterEngine 是 v3 架构中的第一道筛选关卡，替代了 v2 中的 LLM 初筛模块。通过结合 Embedding 语义相似度和 BM25 词频相关性两种评分机制，实现高效、低成本的搜索结果初筛。

### 2.2 Embedding 相似度计算

**实现位置**: [filter/embedding_scorer.py](../../src/web_search/filter/embedding_scorer.py)

Embedding 相似度计算使用余弦相似度算法，将查询文本和搜索结果标题分别编码为向量后计算相似度。

**核心接口**:

```python
class EmbeddingScorer:
    def __init__(self, embedding_client=None):
        from ..core.embedding_client import EmbeddingClientBase, create_embedding_client
        self._embedding_client = embedding_client
        self._default_client: Optional[EmbeddingClientBase] = None

    async def compute_embedding_score(self, query: str, title: str) -> float:
        client = await self._get_client()
        embeddings = await client.encode([query, title])
        return self._cosine_similarity(embeddings[0], embeddings[1])

    async def compute_batch_scores(self, query: str, titles: List[str]) -> List[float]:
        if not titles:
            return []
        client = await self._get_client()
        texts = [query] + titles
        embeddings = await client.encode(texts)
        query_embedding = embeddings[0]
        title_embeddings = embeddings[1:]
        scores = [
            self._cosine_similarity(query_embedding, title_emb)
            for title_emb in title_embeddings
        ]
        return scores

    def _cosine_similarity(self, vec1: np.ndarray, vec2: np.ndarray) -> float:
        dot_product = np.dot(vec1, vec2)
        norm1 = np.linalg.norm(vec1)
        norm2 = np.linalg.norm(vec2)
        if norm1 == 0 or norm2 == 0:
            return 0.0
        return float(dot_product / (norm1 * norm2))
```

**算法说明**:
- 使用 `numpy.dot` 计算向量点积
- 使用 `numpy.linalg.norm` 计算向量范数
- 余弦相似度公式: `cosine_sim = (A · B) / (||A|| × ||B||)`

### 2.3 BM25 词频评分

**实现位置**: [filter/bm25_scorer.py](../../src/web_search/filter/bm25_scorer.py)

BM25 (Best Matching 25) 是一种经典的信息检索评分算法，考虑了文档长度和词频饱和效应。

**核心接口**:

```python
class BM25Scorer:
    def __init__(self, k1: float = 1.5, b: float = 0.75, avg_doc_len: float = None):
        self._k1 = k1
        self._b = b
        self._avg_doc_len = avg_doc_len
        self._doc_freqs: dict = {}
        self._doc_len = 0
        self._num_docs = 0
        self._vocab: set = set()
        self._total_doc_len = 0

    def fit(self, texts: List[str]):
        self._num_docs = len(texts)
        self._doc_freqs = {}
        self._total_doc_len = 0

        for text in texts:
            tokens = self._tokenize(text)
            self._doc_len = len(tokens)
            self._total_doc_len += self._doc_len
            unique_tokens = set(tokens)
            for token in unique_tokens:
                if token not in self._doc_freqs:
                    self._doc_freqs[token] = 0
                self._doc_freqs[token] += 1

        self._avg_doc_len = self._total_doc_len / self._num_docs if self._num_docs > 0 else 0
        self._vocab = set(self._doc_freqs.keys())

    def compute_bm25_score(self, query: str, text: str) -> float:
        if not hasattr(self, '_num_docs') or self._num_docs == 0:
            self.fit([text])

        tokens = self._tokenize(text)
        doc_len = len(tokens)
        score = 0.0

        for term in self._tokenize(query):
            if term not in self._vocab:
                continue

            df = self._doc_freqs.get(term, 0)
            if df == 0:
                continue

            idf = math.log((self._num_docs - df + 0.5) / (df + 0.5) + 1)
            tf = tokens.count(term)
            tf_component = (tf * (self._k1 + 1)) / (tf + self._k1 * (1 - self._b + self._b * doc_len / (self._avg_doc_len + 1e-8)))
            score += idf * tf_component

        return score

    def compute_batch_scores(self, query: str, texts: List[str]) -> List[float]:
        if not texts:
            return []
        self.fit(texts)
        scores = []
        for text in texts:
            score = self.compute_bm25_score(query, text)
            scores.append(score)
        return scores
```

**BM25 公式**:
```
BM25 Score = Σ IDF(term) × (tf × (k1 + 1)) / (tf + k1 × (1 - b + b × |d|/avgdl))

其中:
- IDF(term) = log((N - df + 0.5) / (df + 0.5) + 1)
- tf = 词项在文档中的频率
- |d| = 文档长度
- avgdl = 平均文档长度
- k1 = 1.5 (词频饱和参数)
- b = 0.75 (文档长度归一化参数)
```

### 2.4 混合评价机制

**实现位置**: [filter/hybrid_filter_engine.py](../../src/web_search/filter/hybrid_filter_engine.py)

混合评价机制将 Embedding 相似度和 BM25 得分进行加权融合：

```python
@dataclass
class HybridFilterResult:
    result: SearchResult
    embedding_score: float
    bm25_score: float
    hybrid_score: float

class HybridFilterEngine:
    def __init__(
        self,
        embedding_weight: float = 0.6,
        bm25_weight: float = 0.4,
        embedding_scorer: EmbeddingScorer = None,
        bm25_scorer: BM25Scorer = None,
        min_hybrid_score: float = 0.0
    ):
        self._embedding_weight = embedding_weight
        self._bm25_weight = bm25_weight
        self._embedding_scorer = embedding_scorer or EmbeddingScorer()
        self._bm25_scorer = bm25_scorer or BM25Scorer()
        self._min_hybrid_score = min_hybrid_score

    async def filter(self, results: List[SearchResult], query: str) -> List[HybridFilterResult]:
        if not results:
            return []

        titles = [r.title for r in results]
        content = [r.snippet for r in results]

        embedding_scores = await self._embedding_scorer.compute_batch_scores(query, titles)
        bm25_scorer = BM25Scorer()
        bm25_scores = bm25_scorer.compute_batch_scores(query, titles + content)

        hybrid_results = []
        for i, result in enumerate(results):
            emb_score = embedding_scores[i] if i < len(embedding_scores) else 0.0
            bm_score = bm25_scores[i] if i < len(bm25_scores) else 0.0

            hybrid_score = (
                self._embedding_weight * emb_score +
                self._bm25_weight * bm_score
            )

            hybrid_results.append(HybridFilterResult(
                result=result,
                embedding_score=emb_score,
                bm25_score=bm_score,
                hybrid_score=hybrid_score
            ))

        hybrid_results.sort(key=lambda x: x.hybrid_score, reverse=True)

        return [r for r in hybrid_results if r.hybrid_score >= self._min_hybrid_score]
```

**混合评分公式**:
```
HybridScore = α × EmbeddingScore + β × BM25Score

其中:
- α = 0.6 (Embedding 权重，默认值)
- β = 0.4 (BM25 权重，默认值)
- min_hybrid_score = 0.0 (最低通过阈值)
```

### 2.5 接口定义

```python
# 模块入口
from web_search.filter import HybridFilterEngine, HybridFilterResult

# 使用示例
filter_engine = HybridFilterEngine(
    embedding_weight=0.6,
    bm25_weight=0.4,
    min_hybrid_score=0.3
)

results = await filter_engine.filter(search_results, query)
```

---

## 3. 内容获取器 (ContentFetcher)

### 3.1 模块概述

ContentFetcher 负责从 URL 获取网页内容，并进行文本提取和异常处理。它是 HybridFilterEngine 和 FactExtractor 之间的桥梁。

### 3.2 URL 访问与文本提取

**实现位置**: [fetcher/content_fetcher.py](../../src/web_search/fetcher/content_fetcher.py)

```python
@dataclass
class ContentFetchResult:
    result: SearchResult
    content: str
    fetch_success: bool
    error_reason: Optional[str] = None

class ContentFetcher:
    def __init__(
        self,
        timeout: float = 10.0,
        user_agent: str = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
        max_content_length: int = 100000,
        min_text_length: int = 100
    ):
        self.timeout = timeout
        self.user_agent = user_agent
        self.max_content_length = max_content_length
        self.min_text_length = min_text_length
        self.text_extractor = TextExtractor(
            min_text_length=min_text_length,
            max_text_length=max_content_length
        )

    def fetch(self, results: List[SearchResult]) -> List[ContentFetchResult]:
        fetch_results: List[ContentFetchResult] = []
        for result in results:
            content_result = self._fetch_single(result)
            fetch_results.append(content_result)
            if not content_result.fetch_success:
                logger.warning(
                    f"Failed to fetch content from {result.url}: {content_result.error_reason}"
                )
        return fetch_results

    def _fetch_single(self, result: SearchResult) -> ContentFetchResult:
        try:
            html_content = self._download_url(result.url)
            if html_content is None:
                return ContentFetchResult(
                    result=result,
                    content="",
                    fetch_success=False,
                    error_reason="Failed to download URL"
                )

            extraction_result = self.text_extractor.extract_text(html_content)
            if not extraction_result.success:
                return ContentFetchResult(
                    result=result,
                    content="",
                    fetch_success=False,
                    error_reason=extraction_result.error_reason
                )

            return ContentFetchResult(
                result=result,
                content=extraction_result.text,
                fetch_success=True
            )
        except Exception as e:
            return ContentFetchResult(
                result=result,
                content="",
                fetch_success=False,
                error_reason=f"Unexpected error: {str(e)}"
            )

    def _download_url(self, url: str) -> Optional[str]:
        import urllib.request
        import urllib.error

        request = urllib.request.Request(
            url,
            headers={"User-Agent": self.user_agent}
        )

        try:
            with urllib.request.urlopen(request, timeout=self.timeout) as response:
                content_type = response.headers.get("Content-Type", "")
                if "text/html" not in content_type and "application/xhtml" not in content_type:
                    logger.debug(f"Skipping non-HTML content type: {content_type}")
                    return None

                charset = self._extract_charset(response.headers)
                html_content = response.read()

                if charset:
                    try:
                        html_content = html_content.decode(charset)
                    except UnicodeDecodeError:
                        html_content = html_content.decode("utf-8", errors="replace")
                else:
                    html_content = html_content.decode("utf-8", errors="replace")

                return html_content

        except urllib.error.URLError as e:
            logger.debug(f"URL error for {url}: {e}")
            return None
        except TimeoutError:
            logger.debug(f"Timeout fetching {url}")
            return None
        except Exception as e:
            logger.debug(f"Error downloading {url}: {e}")
            return None
```

### 3.3 文本提取器

**实现位置**: [fetcher/text_extractor.py](../../src/web_search/fetcher/text_extractor.py)

```python
@dataclass
class TextExtractionResult:
    text: str
    success: bool
    error_reason: Optional[str] = None

class TextExtractor:
    def __init__(self, min_text_length: int = 100, max_text_length: int = 100000):
        self.min_text_length = min_text_length
        self.max_text_length = max_text_length

    def extract_text(self, html_content: str) -> TextExtractionResult:
        if not html_content or not html_content.strip():
            return TextExtractionResult(
                text="",
                success=False,
                error_reason="Empty or None HTML content"
            )

        # 优先使用 trafilatura（更智能的正文提取）
        try:
            import trafilatura
            text = trafilatura.extract(
                html_content,
                include_comments=False,
                include_tables=True,
                favor_recall=True
            )
            if text and len(text.strip()) >= self.min_text_length:
                text = text[:self.max_text_length]
                return TextExtractionResult(text=text, success=True)
        except ImportError:
            pass
        except Exception:
            pass

        # 降级使用 BeautifulSoup
        try:
            from bs4 import BeautifulSoup
            soup = BeautifulSoup(html_content, "html.parser")

            # 移除无关标签
            for tag in soup.find_all(["script", "style", "nav", "header", "footer", "aside"]):
                tag.decompose()

            # 尝试定位主体内容
            main_content = soup.find("main") or soup.find("article") or soup.find("body")
            if main_content:
                soup = main_content

            text = soup.get_text(separator="\n", strip=True)
            lines = [line.strip() for line in text.splitlines() if line.strip()]
            text = "\n".join(lines)

            if len(text) < self.min_text_length:
                return TextExtractionResult(
                    text="",
                    success=False,
                    error_reason=f"Extracted text too short: {len(text)} chars"
                )

            text = text[:self.max_text_length]
            return TextExtractionResult(text=text, success=True)

        except ImportError:
            return TextExtractionResult(
                text="",
                success=False,
                error_reason="Neither trafilatura nor BeautifulSoup available"
            )
        except Exception as e:
            return TextExtractionResult(
                text="",
                success=False,
                error_reason=f"Extraction failed: {str(e)}"
            )
```

### 3.4 异常处理机制

ContentFetcher 的异常处理策略：

| 异常类型 | 处理方式 | 日志级别 |
|---------|---------|---------|
| URL 无法访问 | 返回 `fetch_success=False`，`error_reason="Failed to download URL"` | WARNING |
| 非 HTML 内容 | 返回 `fetch_success=False`，跳过该 URL | DEBUG |
| 文本提取失败 | 返回 `fetch_success=False`，记录 `error_reason` | WARNING |
| 超时 | 返回 `fetch_success=False`，`error_reason="Timeout"` | DEBUG |
| 文本过短 | 返回 `fetch_success=False`，`error_reason="Text too short"` | DEBUG |

### 3.5 接口定义

```python
from web_search.fetcher import ContentFetcher, ContentFetchResult

# 使用示例
fetcher = ContentFetcher(
    timeout=10.0,
    max_content_length=100000,
    min_text_length=100
)

fetch_results = fetcher.fetch(search_results)
successful = [r for r in fetch_results if r.fetch_success]
failed = [r for r in fetch_results if not r.fetch_success]
```

---

## 4. 可信度天梯机制 (TrustRankLadder)

### 4.1 模块概述

TrustRankLadder 是 v3 架构中的动态可信度评分系统，替代了 v2 的静态黑白名单。通过 ELO 评分机制，新信源可以逐步积累可信度，而可信度下降的信源也会被相应降级。

### 4.2 ELO 机制原理

**实现位置**: [trust/elo_calculator.py](../../src/web_search/trust/elo_calculator.py)

```python
class ELOCalculator:
    K_FACTOR_NEW = 32
    K_FACTOR_ESTABLISHED = 16

    @staticmethod
    def calculate_expected_score(rating_a: float, rating_b: float) -> float:
        return 1.0 / (1.0 + 10 ** ((rating_b - rating_a) / 400))

    def calculate_new_score(
        self,
        old_score: float,
        opponent_score: float,
        actual_result: float,
        k_factor: int
    ) -> float:
        expected = self.calculate_expected_score(old_score, opponent_score)
        return old_score + k_factor * (actual_result - expected)

    def get_k_factor(self, total_facts: int) -> int:
        return self.K_FACTOR_NEW if total_facts < 50 else self.K_FACTOR_ESTABLISHED

    def calculate_verification_result(self, verification_result: str) -> float:
        result_map = {
            "verified": 1.0,
            "confirmed": 1.0,
            "true": 1.0,
            "correct": 1.0,
            "falsified": 0.0,
            "disproved": 0.0,
            "false": 0.0,
            "wrong": 0.0,
            "neutral": 0.5,
            "unverified": 0.5,
            "unknown": 0.5,
        }
        return result_map.get(verification_result.lower(), 0.5)
```

**ELO 公式**:
```
Expected Score = 1 / (1 + 10^((Rating_B - Rating_A) / 400))

New Rating = Old Rating + K × (Actual Result - Expected Score)

其中:
- Rating_A = 当前信源分数
- Rating_B = 对手信源分数（默认 1200）
- K = 32 (新信源 < 50 facts) 或 16 (老信源)
- Actual Result = 1.0 (验证通过) / 0.0 (证伪) / 0.5 (中性)
```

### 4.3 分数更新规则

**实现位置**: [trust/trust_rank_ladder.py](../../src/web_search/trust/trust_rank_ladder.py)

```python
class TrustLevel(Enum):
    S = "S"   # 1800+
    A = "A"   # 1500-1799
    B = "B"   # 1200-1499
    C = "C"   # 1000-1199
    D = "D"   # 800-999
    E = "E"   # <800

    @staticmethod
    def from_score(score: float) -> "TrustLevel":
        if score >= 1800:
            return TrustLevel.S
        elif score >= 1500:
            return TrustLevel.A
        elif score >= 1200:
            return TrustLevel.B
        elif score >= 1000:
            return TrustLevel.C
        elif score >= 800:
            return TrustLevel.D
        else:
            return TrustLevel.E

@dataclass
class TrustRankScore:
    source_name: str
    source_domain: str
    trust_score: float
    total_facts: int
    verified_facts: int
    falsified_facts: int

    @property
    def trust_level(self) -> TrustLevel:
        return TrustLevel.from_score(self.trust_score)

class TrustRankLadder:
    def __init__(self, storage_path: Optional[str] = None, use_sqlite: bool = False):
        self.elo_calculator = ELOCalculator()
        self.registry = SourceRegistry(
            storage_path=storage_path,
            use_sqlite=use_sqlite
        )
        self.registry.initialize()

    def register_new_source(
        self,
        source_name: str,
        source_domain: str,
        initial_score: float = DEFAULT_INITIAL_SCORE
    ) -> TrustRankScore:
        return self.registry.register(source_name, source_domain, initial_score)

    def get_trust_score(self, source_domain: str) -> float:
        score = self.registry.get_score(source_domain)
        if score is None:
            return DEFAULT_INITIAL_SCORE
        return score.trust_score

    def update_score(
        self,
        source_domain: str,
        verification_result: str,
        opponent_domain: Optional[str] = None,
        opponent_score: Optional[float] = None
    ) -> Optional[TrustRankScore]:
        score = self.registry.get_score(source_domain)
        if score is None:
            return None

        actual_result = self.elo_calculator.calculate_verification_result(verification_result)

        if verification_result.lower() in ["verified", "confirmed", "true", "correct"]:
            score.verified_facts += 1
        elif verification_result.lower() in ["falsified", "disproved", "false", "wrong"]:
            score.falsified_facts += 1

        score.total_facts += 1

        k_factor = self.elo_calculator.get_k_factor(score.total_facts)

        opponent = opponent_score if opponent_score is not None else (
            self.get_trust_score(opponent_domain) if opponent_domain else DEFAULT_OPPONENT_SCORE
        )

        new_score = self.elo_calculator.calculate_new_score(
            old_score=score.trust_score,
            opponent_score=opponent,
            actual_result=actual_result,
            k_factor=k_factor
        )

        score.trust_score = new_score

        self.registry.update_score(score)

        return score
```

### 4.4 天梯等级划分

| 等级 | 分数范围 | 描述 | K 因子 |
|------|---------|------|--------|
| S | 1800+ | 国家级权威媒体 | 16 |
| A | 1500-1799 | 省级权威/知名媒体 | 16 |
| B | 1200-1499 | 市级媒体/专业垂直媒体 | 16 |
| C | 1000-1199 | 普通信源 | 32 |
| D | 800-999 | 待观察信源 | 32 |
| E | <800 | 低可信信源 | 32 |

### 4.5 信源注册与持久化

**实现位置**: [trust/source_registry.py](../../src/web_search/trust/source_registry.py)

```python
DEFAULT_INITIAL_SCORE = 1000.0

class SourceRegistry:
    def __init__(
        self,
        storage_path: Optional[str] = None,
        use_sqlite: bool = False
    ):
        self.storage_path = storage_path
        self.use_sqlite = use_sqlite
        self._scores: Dict[str, TrustRankScore] = {}

        if self.use_sqlite and self.storage_path:
            self._init_sqlite()
        elif self.storage_path:
            self._load_from_json()

    def _init_sqlite(self):
        if not self.storage_path:
            return
        Path(self.storage_path).parent.mkdir(parents=True, exist_ok=True)
        conn = sqlite3.connect(self.storage_path)
        conn.execute("""
            CREATE TABLE IF NOT EXISTS source_registry (
                source_domain TEXT PRIMARY KEY,
                source_name TEXT NOT NULL,
                trust_score REAL NOT NULL,
                total_facts INTEGER DEFAULT 0,
                verified_facts INTEGER DEFAULT 0,
                falsified_facts INTEGER DEFAULT 0
            )
        """)
        conn.commit()
        conn.close()

    def register(
        self,
        source_name: str,
        source_domain: str,
        initial_score: float = DEFAULT_INITIAL_SCORE
    ) -> TrustRankScore:
        if source_domain in self._scores:
            return self._scores[source_domain]

        score = TrustRankScore(
            source_name=source_name,
            source_domain=source_domain,
            trust_score=initial_score,
            total_facts=0,
            verified_facts=0,
            falsified_facts=0
        )
        self._scores[source_domain] = score

        if self.use_sqlite:
            self._save_to_sqlite(score)
        elif self.storage_path:
            self._save_to_json()

        return score
```

### 4.6 接口定义

```python
from web_search.trust import TrustRankLadder, TrustRankScore, TrustLevel

# 使用示例
ladder = TrustRankLadder(storage_path="./data/trust_scores.json", use_sqlite=False)

# 注册新信源
ladder.register_new_source("新浪新闻", "news.sina.com.cn")

# 获取信源分数
score = ladder.get_trust_score("news.sina.com.cn")
level = ladder.get_trust_level("news.sina.com.cn")

# 更新信源分数
ladder.update_score("news.sina.com.cn", "verified", opponent_domain="www.xinhuanet.com")

# 获取排行榜
leaderboard = ladder.get_leaderboard(limit=100)
```

---

## 5. 多因子 Reranker

### 5.1 模块概述

MultiFactorReranker 是 v3 架构中的排序模块，综合考虑时效性、相关性和可信度三个维度进行加权排序，输出 Top 50 结果供后续事实提取使用。

### 5.2 时效性得分计算

**实现位置**: [reranker/freshness_scorer.py](../../src/web_search/reranker/freshness_scorer.py)

```python
class FreshnessScorer:
    def __init__(self, lambda_decay: float = 0.1):
        self.lambda_decay = lambda_decay

    def calculate_freshness_score(self, publish_date: Optional[datetime]) -> float:
        if publish_date is None:
            return 0.5

        try:
            if isinstance(publish_date, str):
                publish_date = datetime.fromisoformat(publish_date.replace("Z", "+00:00"))

            publish_date = publish_date.replace(tzinfo=None)
            days_old = (datetime.now() - publish_date).days

            if days_old < 0:
                return 1.0

            score = math.exp(-self.lambda_decay * days_old)
            return max(0.0, min(1.0, score))
        except Exception:
            return 0.5
```

**时效性评分公式**:
```
FreshnessScore = exp(-λ × days_old)

其中:
- λ = 0.1 (衰减参数，默认值)
- days_old = 当前日期 - 发布日期
- 新内容 (days_old=0): FreshnessScore = 1.0
- 7 天前: FreshnessScore ≈ 0.5
- 30 天前: FreshnessScore ≈ 0.05
```

### 5.3 相关性得分计算

相关性得分直接使用 HybridFilterEngine 输出的混合评分结果进行归一化处理。

```python
def _calc_relevance(self, item: ContentFetchResult) -> float:
    if hasattr(item, 'result') and hasattr(item.result, 'relevance_score'):
        return min(1.0, max(0.0, item.result.relevance_score / 10.0))
    if hasattr(item, 'relevance_score'):
        return min(1.0, max(0.0, item.relevance_score / 10.0))
    return 0.5
```

### 5.4 可信度得分计算

**实现位置**: [reranker/trust_scorer.py](../../src/web_search/reranker/trust_scorer.py)

```python
class TrustScorer:
    def __init__(self, trust_rank_ladder=None):
        self.trust_rank_ladder = trust_rank_ladder
        self._default_scores: Dict[str, float] = {
            "cctv.com": 0.95,
            "xinhuanet.com": 0.95,
            "people.com.cn": 0.93,
            "gov.cn": 0.90,
        }
        self._default_score = 0.5
        self._min_elo = 800
        self._max_elo = 1800

    def get_trust_score(self, source_domain: str) -> float:
        if not source_domain:
            return self._default_score

        source_domain = source_domain.lower()

        if self.trust_rank_ladder is not None:
            try:
                elo_score = self.trust_rank_ladder.get_score(source_domain)
                return self._normalize_elo_score(elo_score)
            except Exception:
                pass

        for trusted_domain, score in self._default_scores.items():
            if trusted_domain in source_domain:
                return score

        return self._default_score

    def _normalize_elo_score(self, elo_score: float) -> float:
        if elo_score <= self._min_elo:
            return 0.0
        if elo_score >= self._max_elo:
            return 1.0
        return (elo_score - self._min_elo) / (self._max_elo - self._min_elo)
```

### 5.5 加权排序算法

**实现位置**: [reranker/multi_factor_reranker.py](../../src/web_search/reranker/multi_factor_reranker.py)

```python
@dataclass
class RerankConfig:
    weights: Dict[str, float]
    freshness_lambda: float
    top_k: int

    @classmethod
    def from_dict(cls, config: Dict[str, Any]) -> "RerankConfig":
        weights = config.get("weights", {
            "freshness": 0.3,
            "relevance": 0.4,
            "trust": 0.3
        })
        freshness_lambda = config.get("freshness", {}).get("lambda", 0.1)
        top_k = config.get("top_k", 50)
        return cls(
            weights=weights,
            freshness_lambda=freshness_lambda,
            top_k=top_k
        )

class MultiFactorReranker:
    def __init__(self, config: Optional[RerankConfig] = None):
        if config is None:
            config = RerankConfig(
                weights={"freshness": 0.3, "relevance": 0.4, "trust": 0.3},
                freshness_lambda=0.1,
                top_k=50
            )
        self.config = config
        self.freshness_scorer = FreshnessScorer(lambda_decay=config.freshness_lambda)
        self.trust_scorer = TrustScorer()

    def rerank(self, results: List[ContentFetchResult], query: str) -> List[ContentFetchResult]:
        if not results:
            return []

        scored_results = []
        for item in results:
            freshness_score = self._calc_freshness(item)
            relevance_score = self._calc_relevance(item)
            trust_score = self._calc_trust(item)

            final_score = (
                self.config.weights.get("freshness", 0.3) * freshness_score +
                self.config.weights.get("relevance", 0.4) * relevance_score +
                self.config.weights.get("trust", 0.3) * trust_score
            )

            scored_results.append((item, final_score))

        scored_results.sort(key=lambda x: x[1], reverse=True)

        top_results = [item for item, _ in scored_results[:self.config.top_k]]

        for i, (item, score) in enumerate(scored_results[:self.config.top_k]):
            if hasattr(item, 'result'):
                item.result.final_score = score
            elif hasattr(item, 'final_score'):
                item.final_score = score

        return top_results
```

**综合评分公式**:
```
FinalScore = w_freshness × FreshnessScore + w_relevance × RelevanceScore + w_trust × TrustScore

其中:
- w_freshness = 0.3 (时效性权重)
- w_relevance = 0.4 (相关性权重)
- w_trust = 0.3 (可信度权重)
- FreshnessScore ∈ [0, 1] (基于时间的衰减分数)
- RelevanceScore ∈ [0, 1] (来自 HybridFilterEngine 的混合评分)
- TrustScore ∈ [0, 1] (来自 TrustRankLadder 的 ELO 归一化分数)
```

### 5.6 接口定义

```python
from web_search.reranker import MultiFactorReranker, RerankConfig, ContentFetchResult

# 使用示例
config = RerankConfig(
    weights={"freshness": 0.3, "relevance": 0.4, "trust": 0.3},
    freshness_lambda=0.1,
    top_k=50
)

reranker = MultiFactorReranker(config=config)
reranked = reranker.rerank(fetch_results, query)
```

---

## 6. 事实提取器 (FactExtractor)

### 6.1 模块概述

FactExtractor 是 v3 架构中的核心事实理解模块，负责从正文内容中提取结构化的事实信息，包括 NLI 分析、SPO 三元组提取、数值和时间信息提取。

### 6.2 事实列表提取

**实现位置**: [extractor/fact_extractor.py](../../src/web_search/extractor/fact_extractor.py)

```python
@dataclass
class ExtractedFact:
    fact_id: str
    statement: str
    spo_triple: Optional[SPOTriple] = None
    nli_label: Optional[str] = None
    numeric_values: List[NumericValue] = field(default_factory=list)
    datetime_values: List[DatetimeValue] = field(default_factory=list)
    confidence_score: float = 0.0
    confidence_reason: str = ""
    source: Optional[TrustRankScore] = None

class FactExtractor:
    def __init__(
        self,
        nli_analyzer: Optional[NLIAnalyzer] = None,
        spo_extractor: Optional[SPOExtractor] = None,
        value_extractor: Optional[ValueExtractor] = None,
        llm_client: Optional[LLMClientBase] = None,
        settings: Optional[Settings] = None,
        min_confidence_threshold: float = 0.3,
        max_facts_per_content: int = 50
    ):
        self._nli_analyzer = nli_analyzer or NLIAnalyzer()
        self._spo_extractor = spo_extractor or SPOExtractor()
        self._value_extractor = value_extractor or ValueExtractor()
        self._llm_client = llm_client
        self._settings = settings
        self._min_confidence_threshold = min_confidence_threshold
        self._max_facts_per_content = max_facts_per_content

    def extract_sync(self, content: str, source_domain: str) -> List[ExtractedFact]:
        sentences = self._split_into_sentences(content)
        facts: List[ExtractedFact] = []

        for sentence in sentences:
            if not self._is_factual_sentence(sentence):
                continue

            spo_triples = self._spo_extractor.extract_spo_sync(sentence)
            numeric_values, datetime_values = self._value_extractor.extract_values_sync(sentence)

            for triple in spo_triples:
                fact = self._create_fact_from_triple(
                    triple=triple,
                    source_domain=source_domain,
                    original_sentence=sentence
                )
                fact.numeric_values = numeric_values
                fact.datetime_values = datetime_values
                facts.append(fact)

            if not spo_triples and (numeric_values or datetime_values):
                fact = self._create_fact_from_values(
                    sentence=sentence,
                    numeric_values=numeric_values,
                    datetime_values=datetime_values,
                    source_domain=source_domain
                )
                facts.append(fact)

            if len(facts) >= self._max_facts_per_content:
                break

        facts = self._compute_confidence_scores(facts)
        facts = [f for f in facts if f.confidence_score >= self._min_confidence_threshold]
        return facts

    def _split_into_sentences(self, content: str) -> List[str]:
        content = content.replace('\n', ' ').replace('\r', '')
        sentence_endings = re.compile(r'(?<=[。！？；?!.?!;])\s+')
        sentences = sentence_endings.split(content)
        sentences = [s.strip() for s in sentences if s.strip() and len(s.strip()) > 10]
        return sentences

    def _is_factual_sentence(self, sentence: str) -> bool:
        if len(sentence) < 10 or len(sentence) > 500:
            return False

        non_factual_patterns = [
            r'^请问', r'^如果', r'^虽然', r'^但是', r'^然而',
            r'^可能', r'^也许', r'^大概', r'吗\?$', r'呢\?$', r'？$',
        ]
        for pattern in non_factual_patterns:
            if re.search(pattern, sentence):
                return False

        factual_indicators = [
            r'\d+', r'年', r'月', r'日', r'据', r'报道',
            r'表示', r'说', r'宣布', r'证实', r'确认',
        ]
        for indicator in factual_indicators:
            if re.search(indicator, sentence):
                return True

        return False

    def _compute_confidence_scores(self, facts: List[ExtractedFact]) -> List[ExtractedFact]:
        for fact in facts:
            score = 0.5
            reasons = []

            if fact.spo_triple:
                score += 0.15
                reasons.append("包含完整的SPO三元组结构")

            if fact.numeric_values:
                score += 0.1
                reasons.append(f"包含{len(fact.numeric_values)}个数值")

            if fact.datetime_values:
                score += 0.1
                reasons.append(f"包含{len(fact.datetime_values)}个时间信息")

            if fact.nli_label == "entailment":
                score += 0.15
                reasons.append("NLI分析为 entailment")
            elif fact.nli_label == "contradiction":
                score -= 0.2
                reasons.append("NLI分析为 contradiction")

            subject = fact.spo_triple.subject if fact.spo_triple else ""
            if self._is_specific_entity(subject):
                score += 0.1
                reasons.append("主语为具体实体")

            score = max(0.0, min(1.0, score))

            fact.confidence_score = score
            fact.confidence_reason = "; ".join(reasons) if reasons else "基础置信度"

        return facts
```

### 6.3 NLI 分析

**实现位置**: [extractor/nli_analyzer.py](../../src/web_search/extractor/nli_analyzer.py)

NLI (Natural Language Inference) 分析用于判断两个陈述之间的语义关系。

```python
class NLIAnalyzer:
    def __init__(
        self,
        model_name: str = "cross-encoder/nli-deberta-v3-small",
        use_llm_fallback: bool = True,
        llm_client: Optional[LLMClientBase] = None,
        settings: Optional[Settings] = None
    ):
        self._model_name = model_name
        self._use_llm_fallback = use_llm_fallback
        self._cross_encoder = None
        self._llm_client = llm_client
        self._settings = settings

    def analyze_nli_sync(self, statement1: str, statement2: str) -> str:
        model = self._load_cross_encoder()
        if model is not None:
            scores = model.predict([(statement1, statement2)])
            return self._scores_to_label(scores[0])
        if self._use_llm_fallback:
            return self._analyze_with_llm(statement1, statement2)
        return "neutral"

    def _scores_to_label(self, scores) -> str:
        if isinstance(scores, np.ndarray):
            scores = scores.tolist()
        if isinstance(scores, (list, tuple)) and len(scores) >= 3:
            label_idx = int(np.argmax(scores))
            labels = ["entailment", "neutral", "contradiction"]
            return labels[label_idx] if label_idx < len(labels) else "neutral"
        if isinstance(scores, (list, tuple)) and len(scores) == 1:
            score = scores[0]
            if score > 0.5:
                return "entailment"
            elif score < -0.5:
                return "contradiction"
            return "neutral"
        return "neutral"

    def _analyze_with_llm(self, statement1: str, statement2: str) -> str:
        prompt = f"""Given two statements, determine the NLI relationship:
Statement 1: {statement1}
Statement 2: {statement2}

Classify the relationship as one of:
- entailment: Statement 2 is supported by Statement 1
- neutral: Statement 2 is neither supported nor contradicted by Statement 1
- contradiction: Statement 2 contradicts Statement 1

Respond with only one word: entailment, neutral, or contradiction."""

        try:
            response = self._llm_client.complete_sync(prompt).strip().lower()
            if response in ["entailment", "neutral", "contradiction"]:
                return response
            return "neutral"
        except Exception:
            return "neutral"
```

**NLI 标签定义**:

| 标签 | 含义 | 置信度调整 |
|------|------|-----------|
| entailment | 陈述2由陈述1支持 | +0.15 |
| neutral | 陈述2与陈述1无关 | 无变化 |
| contradiction | 陈述2与陈述1矛盾 | -0.20 |

### 6.4 SPO 三元组提取

**实现位置**: [extractor/spo_extractor.py](../../src/web_search/extractor/spo_extractor.py)

```python
@dataclass
class SPOTriple:
    subject: str
    predicate: str
    object: str

    def to_dict(self) -> dict:
        return {
            "subject": self.subject,
            "predicate": self.predicate,
            "object": self.object
        }

    def __str__(self) -> str:
        return f"{self.subject} | {self.predicate} | {self.object}"

class SPOExtractor:
    def __init__(
        self,
        spacy_model: str = "en_core_web_sm",
        use_llm_fallback: bool = True,
        llm_client: Optional[LLMClientBase] = None,
        settings: Optional[Settings] = None
    ):
        self._spacy_model = spacy_model
        self._use_llm_fallback = use_llm_fallback
        self._nlp = None
        self._llm_client = llm_client
        self._settings = settings

    def extract_spo_sync(self, text: str) -> List[SPOTriple]:
        nlp = self._load_spacy()
        if nlp is not None:
            return self._extract_with_spacy(text)
        if self._use_llm_fallback:
            return self._extract_with_llm(text)
        return self._extract_simple_patterns(text)

    def _extract_with_spacy(self, text: str) -> List[SPOTriple]:
        triples: List[SPOTriple] = []
        doc = self._nlp(text)

        for sent in doc.sents:
            subject = None
            predicate = None
            obj = None

            for token in sent:
                if token.dep_ in ("nsubj", "nsubjpass"):
                    subject = self._get_subtree_text(token)
                elif token.dep_ == "ROOT":
                    predicate = token.lemma_
                elif token.dep_ in ("dobj", "pobj", "attr"):
                    obj = self._get_subtree_text(token)

            if subject and predicate and obj:
                triples.append(SPOTriple(
                    subject=self._clean_text(subject),
                    predicate=self._clean_text(predicate),
                    object=self._clean_text(obj)
                ))

        return triples

    def _extract_with_llm(self, text: str) -> List[SPOTriple]:
        prompt = f"""Extract Subject-Predicate-Object triples from the following text.
Return the triples in the format: subject | predicate | object
One triple per line. If no triples found, return "NONE".

Text: {text}

Triples:"""

        try:
            response = self._llm_client.complete_sync(prompt)
            return self._parse_llm_response(response)
        except Exception:
            return self._extract_simple_patterns(text)
```

### 6.5 数值/时间提取

**实现位置**: [extractor/value_extractor.py](../../src/web_search/extractor/value_extractor.py)

```python
@dataclass
class NumericValue:
    value: float
    unit: Optional[str]
    context: str

    def to_dict(self) -> dict:
        return {
            "value": self.value,
            "unit": self.unit,
            "context": self.context
        }

@dataclass
class DatetimeValue:
    date: datetime
    context: str

    def to_dict(self) -> dict:
        return {
            "date": self.date.isoformat(),
            "context": self.context
        }

class ValueExtractor:
    PERCENTAGE_PATTERNS = [
        r'(\d+(?:\.\d+)?)\s*%',
        r'(\d+(?:\.\d+)?)\s*percent(?:age)?',
        r'(\d+(?:\.\d+)?)\s*个百分点',
    ]

    NUMBER_PATTERNS = [
        r'(\d+(?:\.\d+)?)\s*(万|亿|千|百|百万|千万|十|个|件|名|位|人次|人|元|美元|欧元|英镑|日元)?',
        r'(\d+(?:,\d{3})*(?:\.\d+)?)',
    ]

    DATE_PATTERNS = [
        (r'(\d{4})[-/年](\d{1,2})[-/月](\d{1,2})[日号]?', '%Y-%m-%d'),
        (r'(\d{4})[-/年](\d{1,2})[-/月]', '%Y-%m'),
        (r'(\d{4})年', '%Y'),
        (r'(\d{1,2})[-/月](\d{1,2})[日号]?', '%m-%d'),
        (r'(?:上|下)半年', None),
        (r'(?:第[一二三四]季度|Q[1-4])', None),
    ]

    def extract_values_sync(self, text: str) -> Tuple[List[NumericValue], List[DatetimeValue]]:
        numeric_values = self._extract_numeric_values(text)
        datetime_values = self._extract_datetime_values(text)
        return numeric_values, datetime_values

    def _extract_numeric_values(self, text: str) -> List[NumericValue]:
        numeric_values: List[NumericValue] = []

        for match in self._percent_re.finditer(text):
            value_str = match.group(1)
            try:
                value = float(value_str)
                context = self._get_context(text, match.start(), match.end())
                numeric_values.append(NumericValue(
                    value=value,
                    unit="%",
                    context=context
                ))
            except ValueError:
                continue

        # ... 更多提取逻辑

        numeric_values.sort(key=lambda x: abs(x.value), reverse=True)
        return numeric_values

    def _extract_datetime_values(self, text: str) -> List[DatetimeValue]:
        datetime_values: List[DatetimeValue] = []

        for pattern, date_format in self.DATE_PATTERNS:
            regex = re.compile(pattern, re.IGNORECASE)
            for match in regex.finditer(text):
                date_obj = self._parse_date_match(match, date_format)
                if date_obj is not None:
                    context = self._get_context(text, match.start(), match.end())
                    datetime_values.append(DatetimeValue(
                        date=date_obj,
                        context=context
                    ))

        # 处理相对日期
        special_date_patterns = [
            (r'昨天', lambda: self._days_ago(1)),
            (r'今天', lambda: self._days_ago(0)),
            (r'明天', lambda: self._days_ago(-1)),
            # ... 更多模式
        ]

        datetime_values.sort(key=lambda x: x.date, reverse=True)
        return datetime_values
```

### 6.6 接口定义

```python
from web_search.extractor import FactExtractor, ExtractedFact

extractor = FactExtractor(
    min_confidence_threshold=0.3,
    max_facts_per_content=50
)

facts = extractor.extract_sync(content, source_domain="news.example.com")

for fact in facts:
    print(f"ID: {fact.fact_id}")
    print(f"Statement: {fact.statement}")
    print(f"SPO: {fact.spo_triple}")
    print(f"NLI: {fact.nli_label}")
    print(f"Confidence: {fact.confidence_score}")
    print("---")
```

---

## 7. 事实桶聚类 (FactBucketCluster)

### 7.1 模块概述

FactBucketCluster 负责将提取的事实按照语义相似度进行聚类，形成不同的事实桶（FactBucket），为后续的碰撞检测提供基础。

### 7.2 Embedding 相似度计算

**实现位置**: [cluster/fact_bucket_cluster.py](../../src/web_search/cluster/fact_bucket_cluster.py)

```python
@dataclass
class ExtractedFact:
    fact_id: str
    statement: str
    spo_triple: Optional[SPOTriple] = None
    nli_label: Optional[NLILabel] = None
    numeric_values: List[NumericValue] = field(default_factory=list)
    datetime_values: List[DatetimeValue] = field(default_factory=list)
    confidence_score: float = 0.0
    confidence_reason: Optional[str] = None
    source_domain: Optional[str] = None
    source_name: Optional[str] = None

@dataclass
class FactBucket:
    bucket_id: str
    facts: List[ExtractedFact]
    cluster_embedding: List[float]

    @staticmethod
    def generate_id() -> str:
        return str(uuid.uuid4())[:8]

class FactBucketCluster:
    def __init__(
        self,
        embedding_client=None,
        similarity_threshold: float = 0.75,
        use_hierarchical: bool = True
    ):
        self._embedding_client = embedding_client
        self._similarity_threshold = similarity_threshold
        self._use_hierarchical = use_hierarchical

    async def cluster(self, facts: List[ExtractedFact]) -> List[FactBucket]:
        if not facts:
            return []

        if len(facts) == 1:
            embedding = await self._compute_embedding([facts[0].statement])
            return [FactBucket(
                bucket_id=FactBucket.generate_id(),
                facts=facts,
                cluster_embedding=embedding[0].tolist()
            )]

        statements = [f.statement for f in facts]
        embeddings = await self._compute_embedding(statements)

        similarity_matrix = self._compute_similarity_matrix(embeddings)

        if self._use_hierarchical:
            clusters = self._hierarchical_clustering(similarity_matrix, facts, embeddings)
        else:
            clusters = self._greedy_clustering(similarity_matrix, facts, embeddings)

        result = []
        for cluster_facts, cluster_emb in clusters:
            result.append(FactBucket(
                bucket_id=FactBucket.generate_id(),
                facts=cluster_facts,
                cluster_embedding=cluster_emb.tolist()
            ))

        return result

    def _compute_similarity_matrix(self, embeddings: List) -> List[List[float]]:
        n = len(embeddings)
        matrix = [[0.0] * n for _ in range(n)]
        for i in range(n):
            for j in range(i, n):
                sim = self._cosine_similarity(embeddings[i], embeddings[j])
                matrix[i][j] = sim
                matrix[j][i] = sim
        return matrix

    def _cosine_similarity(self, vec1, vec2) -> float:
        import numpy as np
        v1 = np.array(vec1)
        v2 = np.array(vec2)
        dot_product = np.dot(v1, v2)
        norm1 = np.linalg.norm(v1)
        norm2 = np.linalg.norm(v2)
        if norm1 == 0 or norm2 == 0:
            return 0.0
        return float(dot_product / (norm1 * norm2))
```

### 7.3 桶划分算法

系统提供两种聚类算法：

**层次聚类 (Hierarchical Clustering)**:

```python
def _hierarchical_clustering(
    self,
    similarity_matrix: List[List[float]],
    facts: List[ExtractedFact],
    embeddings: List
) -> List[tuple]:
    n = len(facts)
    if n == 0:
        return []

    import numpy as np
    distance_matrix = 1 - np.array(similarity_matrix)
    np.fill_diagonal(distance_matrix, 0)

    from scipy.cluster.hierarchy import linkage, fcluster
    from scipy.spatial.distance import squareform

    condensed_dist = squareform(distance_matrix)
    Z = linkage(condensed_dist, method='average')

    clusters = fcluster(Z, t=1 - self._similarity_threshold, criterion='distance')

    cluster_map = {}
    for idx, cluster_id in enumerate(clusters):
        if cluster_id not in cluster_map:
            cluster_map[cluster_id] = []
        cluster_map[cluster_id].append(idx)

    result = []
    for cluster_id, indices in cluster_map.items():
        cluster_facts = [facts[i] for i in indices]
        cluster_embs = [embeddings[i] for i in indices]
        centroid = np.mean(cluster_embs, axis=0)
        result.append((cluster_facts, centroid))

    return result
```

**贪心聚类 (Greedy Clustering)**:

```python
def _greedy_clustering(
    self,
    similarity_matrix: List[List[float]],
    facts: List[ExtractedFact],
    embeddings: List
) -> List[tuple]:
    n = len(facts)
    if n == 0:
        return []

    assigned = [False] * n
    clusters = []

    for i in range(n):
        if assigned[i]:
            continue

        cluster_indices = [i]
        assigned[i] = True

        for j in range(i + 1, n):
            if assigned[j]:
                continue
            sim = similarity_matrix[i][j]
            if sim >= self._similarity_threshold:
                cluster_indices.append(j)
                assigned[j] = True

        cluster_facts = [facts[idx] for idx in cluster_indices]
        cluster_embs = [embeddings[idx] for idx in cluster_indices]

        import numpy as np
        centroid = np.mean(cluster_embs, axis=0)
        clusters.append((cluster_facts, centroid))

    unassigned = [i for i in range(n) if not assigned[i]]
    for idx in unassigned:
        clusters.append(([facts[idx]], embeddings[idx]))

    return clusters
```

### 7.4 接口定义

```python
from web_search.cluster import FactBucketCluster, FactBucket

clusterer = FactBucketCluster(
    similarity_threshold=0.75,
    use_hierarchical=True
)

buckets = await clusterer.cluster(extracted_facts)

for bucket in buckets:
    print(f"Bucket ID: {bucket.bucket_id}")
    print(f"Fact Count: {len(bucket.facts)}")
    print("---")
```

---

## 8. 正交碰撞检测器 (OrthogonalCollisionDetector)

### 8.1 模块概述

OrthogonalCollisionDetector 是 v3 架构中的核心碰撞检测模块，通过 NLI、SPO、数值/时间三个独立维度进行正交检测，计算碰撞系数，并根据阈值决定是否触发 LLM 复核。

### 8.2 碰撞检测配置

**实现位置**: [collision/orthogonal_detector.py](../../src/web_search/collision/orthogonal_detector.py)

```python
@dataclass
class OrthogonalCollisionConfig:
    alpha: float = 0.4      # NLI 冲突权重
    beta: float = 0.3       # SPO 冲突权重
    gamma: float = 0.3      # 数值/时间冲突权重
    threshold: float = 0.3   # 碰撞系数阈值
    nli_threshold: float = 0.5
    spo_similarity_threshold: float = 0.8
    numeric_threshold: float = 0.2
    datetime_threshold_days: int = 1

DEFAULT_CONFIG = OrthogonalCollisionConfig()

class OrthogonalCollisionDetector:
    def __init__(
        self,
        config: Optional[OrthogonalCollisionConfig] = None,
        llm_client: Optional[LLMClientBase] = None
    ):
        self.config = config or DEFAULT_CONFIG
        self.llm_client = llm_client

        self.nli_detector = NLICollisionDetector(threshold=self.config.nli_threshold)
        self.spo_detector = SPOCollisionDetector(similarity_threshold=self.config.spo_similarity_threshold)
        self.value_detector = ValueCollisionDetector(
            numeric_threshold=self.config.numeric_threshold,
            datetime_threshold_days=self.config.datetime_threshold_days
        )

    def detect(self, bucket: FactBucket) -> CollisionResult:
        if not bucket.facts or len(bucket.facts) < 2:
            return CollisionResult(
                bucket_id=bucket.bucket_id,
                collision_coefficient=0.0,
                conflicting_facts=[],
                supporting_facts=bucket.facts,
                needs_llm_review=False
            )

        nli_conflict_ratio = self.nli_detector.detect_nli_collision(bucket.facts)
        spo_conflict_ratio = self.spo_detector.detect_spo_collision(bucket.facts)
        value_conflict_ratio = self.value_detector.detect_value_collision(bucket.facts)

        collision_coefficient = (
            self.config.alpha * nli_conflict_ratio +
            self.config.beta * spo_conflict_ratio +
            self.config.gamma * value_conflict_ratio
        )

        conflicting_facts = self._get_all_conflicting_facts(bucket.facts)
        supporting_facts = [f for f in bucket.facts if f not in conflicting_facts]

        needs_llm_review = collision_coefficient >= self.config.threshold
        llm_review_result = None

        if needs_llm_review and self.llm_client:
            llm_review_result = self._perform_llm_review(bucket, collision_coefficient)

        return CollisionResult(
            bucket_id=bucket.bucket_id,
            collision_coefficient=collision_coefficient,
            conflicting_facts=conflicting_facts,
            supporting_facts=supporting_facts,
            needs_llm_review=needs_llm_review,
            llm_review_result=llm_review_result,
            nli_conflict_ratio=nli_conflict_ratio,
            spo_conflict_ratio=spo_conflict_ratio,
            value_conflict_ratio=value_conflict_ratio
        )
```

### 8.3 NLI 冲突检测

**实现位置**: [collision/nli_collision.py](../../src/web_search/collision/nli_collision.py)

```python
class NLICollisionDetector:
    def __init__(self, threshold: float = 0.5):
        self.threshold = threshold

    def detect_nli_collision(self, facts: List[ExtractedFact]) -> float:
        if len(facts) < 2:
            return 0.0

        contradictions = 0
        total_pairs = 0

        for i in range(len(facts)):
            for j in range(i + 1, len(facts)):
                fact_a = facts[i]
                fact_b = facts[j]

                if self._is_contradiction(fact_a, fact_b):
                    contradictions += 1

                total_pairs += 1

        if total_pairs == 0:
            return 0.0

        return contradictions / total_pairs

    def _is_contradiction(self, fact_a: ExtractedFact, fact_b: ExtractedFact) -> bool:
        if fact_a.nli_label and fact_b.nli_label:
            if fact_a.nli_label == NLILabel.CONTRADICTION.value and fact_b.nli_label == NLILabel.CONTRADICTION.value:
                return True

            if fact_a.nli_label == NLILabel.ENTAILMENT.value and fact_b.nli_label == NLILabel.CONTRADICTION.value:
                return True
            if fact_a.nli_label == NLILabel.CONTRADICTION.value and fact_b.nli_label == NLILabel.ENTAILMENT.value:
                return True

        return self._check_semantic_contradiction(fact_a.statement, fact_b.statement)

    def _check_semantic_contradiction(self, stmt_a: str, stmt_b: str) -> bool:
        negation_keywords = [
            "不是", "非", "没", "无", "未", "别", "否",
            "not", "no", "never", "none", "neither",
            "false", "wrong", "incorrect", "deny", "reject"
        ]

        stmt_a_lower = stmt_a.lower()
        stmt_b_lower = stmt_b.lower()

        for keyword in negation_keywords:
            if keyword in stmt_a_lower and keyword in stmt_b_lower:
                if self._are_opposite_statements(stmt_a_lower, stmt_b_lower, keyword):
                    return True

        return False
```

### 8.4 SPO 冲突检测

**实现位置**: [collision/spo_collision.py](../../src/web_search/collision/spo_collision.py)

```python
class SPOCollisionDetector:
    def __init__(self, similarity_threshold: float = 0.8):
        self.similarity_threshold = similarity_threshold

    def detect_spo_collision(self, facts: List[ExtractedFact]) -> float:
        if len(facts) < 2:
            return 0.0

        spo_groups = self._group_by_subject(facts)

        conflicts = 0
        total_comparisons = 0

        for subject, group_facts in spo_groups.items():
            if len(group_facts) < 2:
                continue

            for i in range(len(group_facts)):
                for j in range(i + 1, len(group_facts)):
                    fact_a = group_facts[i]
                    fact_b = group_facts[j]

                    if self._has_spo_conflict(fact_a, fact_b):
                        conflicts += 1

                    total_comparisons += 1

        if total_comparisons == 0:
            return 0.0

        return conflicts / total_comparisons

    def _predicates_opposite(self, pred_a: str, pred_b: str) -> bool:
        opposite_pairs = [
            ("是", "不是"), ("在", "不在"), ("有", "没有"),
            ("能", "不能"), ("会", "不会"), ("支持", "反对"),
            ("赞成", "反对"), ("承认", "否认"), ("确认", "否认"),
            ("上升", "下降"), ("增加", "减少"), ("买入", "卖出"),
            ("喜欢", "讨厌"), ("同意", "不同意"),
        ]

        pred_a_norm = pred_a.lower().strip()
        pred_b_norm = pred_b.lower().strip()

        for pos, neg in opposite_pairs:
            if (pos in pred_a_norm and neg in pred_b_norm) or (neg in pred_a_norm and pos in pred_b_norm):
                return True

        return False

    def _objects_conflict(self, obj_a: str, obj_b: str) -> bool:
        norm_a = self._normalize_entity(obj_a)
        norm_b = self._normalize_entity(obj_b)

        if norm_a == norm_b:
            return False

        import difflib
        similarity = difflib.SequenceMatcher(None, norm_a, norm_b).ratio()

        if similarity >= self.similarity_threshold:
            return False

        if self._is_numeric_or_boolean_conflict(obj_a, obj_b):
            return True

        antonym_pairs = [
            ("是", "否"), ("有", "无"), ("真", "假"), ("对", "错"),
            ("true", "false"), ("yes", "no"), ("correct", "wrong"),
        ]

        for pos, neg in antonym_pairs:
            if (pos in norm_a and neg in norm_b) or (neg in norm_a and pos in norm_b):
                return True

        return False
```

### 8.5 数值/时间冲突检测

**实现位置**: [collision/value_collision.py](../../src/web_search/collision/value_collision.py)

```python
class ValueCollisionDetector:
    def __init__(
        self,
        numeric_threshold: float = 0.2,
        datetime_threshold_days: int = 1
    ):
        self.numeric_threshold = numeric_threshold
        self.datetime_threshold_days = datetime_threshold_days

    def detect_value_collision(self, facts: List[ExtractedFact]) -> float:
        if len(facts) < 2:
            return 0.0

        numeric_groups = self._group_by_context(facts)
        datetime_groups = self._group_by_datetime_context(facts)

        conflicts = 0
        total_comparisons = 0

        for context, group_facts in numeric_groups.items():
            if len(group_facts) < 2:
                continue
            for i in range(len(group_facts)):
                for j in range(i + 1, len(group_facts)):
                    if self._has_numeric_conflict(group_facts[i], group_facts[j]):
                        conflicts += 1
                    total_comparisons += 1

        for context, group_facts in datetime_groups.items():
            if len(group_facts) < 2:
                continue
            for i in range(len(group_facts)):
                for j in range(i + 1, len(group_facts)):
                    if self._has_datetime_conflict(group_facts[i], group_facts[j]):
                        conflicts += 1
                    total_comparisons += 1

        if total_comparisons == 0:
            return 0.0

        return conflicts / total_comparisons

    def _has_numeric_conflict(self, fact_a: ExtractedFact, fact_b: ExtractedFact) -> bool:
        if not fact_a.numeric_values or not fact_b.numeric_values:
            return False

        for num_a in fact_a.numeric_values:
            for num_b in fact_b.numeric_values:
                if self._values_conflict(num_a, num_b):
                    return True

        return False

    def _values_conflict(self, num_a: NumericValue, num_b: NumericValue) -> bool:
        if num_a.unit != num_b.unit:
            if not self._are_compatible_units(num_a.unit, num_b.unit):
                return False

        try:
            val_a = float(num_a.value)
            val_b = float(num_b.value)

            if val_a == 0 and val_b == 0:
                return False

            larger = max(abs(val_a), abs(val_b))
            if larger > 0:
                relative_diff = abs(val_a - val_b) / larger
                if relative_diff > self.numeric_threshold:
                    return True

            return val_a != val_b
        except (ValueError, TypeError):
            return num_a.value != num_b.value
```

### 8.6 LLM 复核机制

```python
def _perform_llm_review(
    self,
    bucket: FactBucket,
    collision_coefficient: float
) -> Optional[str]:
    if not self.llm_client:
        return None

    try:
        conflicting_facts = self._get_all_conflicting_facts(bucket.facts)
        prompt = self._build_llm_review_prompt(bucket, conflicting_facts, collision_coefficient)
        review_result = self.llm_client.complete_sync(prompt)
        return review_result

    except Exception as e:
        logger.error(f"LLM review failed: {e}")
        return None

def _build_llm_review_prompt(
    self,
    bucket: FactBucket,
    conflicting_facts: List[ExtractedFact],
    collision_coefficient: float
) -> str:
    facts_text = "\n".join([
        f"- [{fact.fact_id}] {fact.statement} (source: {fact.source_domain or 'unknown'}, trust: {fact.trust_score:.1f})"
        for fact in bucket.facts
    ])

    conflicting_text = "\n".join([
        f"- [{fact.fact_id}] {fact.statement}"
        for fact in conflicting_facts
    ])

    prompt = f"""你是一个事实核查专家。请分析以下事实桶中的冲突情况。

事实桶 ID: {bucket.bucket_id}
碰撞系数: {collision_coefficient:.3f} (阈值: {self.config.threshold})

所有事实:
{facts_text}

存在冲突的事实:
{conflicting_text}

请分析:
1. 哪些事实是可信的（被多个可靠来源支持）？
2. 哪些事实是存疑的或错误的？
3. 最终的事实判定是什么？

请用中文回复，格式如下：
- 可信事实：[列出可信的事实ID和理由]
- 存疑事实：[列出存疑的事实ID和理由]
- 最终判定：[给出最终的事实结论]
"""
    return prompt
```

### 8.7 碰撞系数计算

**碰撞系数公式**:
```
CollisionCoefficient = α × NLI_Conflict + β × SPO_Conflict + γ × Value_Conflict

其中:
- α = 0.4 (NLI 冲突权重)
- β = 0.3 (SPO 冲突权重)
- γ = 0.3 (数值/时间冲突权重)
- NLI_Conflict = NLI 检测到的 Contradiction 比例
- SPO_Conflict = SPO 三元组中主语相同但谓语/宾语冲突的比例
- Value_Conflict = 数值或时间矛盾的比例
```

**阈值判定**:

| 碰撞系数 | 判定结果 | 处理方式 |
|---------|---------|---------|
| < 0.3 | 低碰撞 | 直接标记为可信事实 |
| >= 0.3 | 高碰撞 | 触发 LLM 复核 |

### 8.8 接口定义

```python
from web_search.collision import OrthogonalCollisionDetector, CollisionResult, TrustedFact

detector = OrthogonalCollisionDetector(
    config=OrthogonalCollisionConfig(
        alpha=0.4,
        beta=0.3,
        gamma=0.3,
        threshold=0.3
    ),
    llm_client=llm_client
)

result = detector.detect(fact_bucket)

print(f"Collision Coefficient: {result.collision_coefficient}")
print(f"Needs LLM Review: {result.needs_llm_review}")
print(f"Conflicting Facts: {len(result.conflicting_facts)}")
print(f"Supporting Facts: {len(result.supporting_facts)}")

trusted_facts = detector.get_trusted_facts(buckets)
```

---

## 9. SearchOrchestrator v3 完整流程

### 9.1 完整流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SearchOrchestrator v3                             │
└─────────────────────────────────────────────────────────────────────────────┘

流程步骤:

1. QueryRewriter.rewrite(query)
   ├── 意图理解 (Intent Analysis)
   ├── 查询发散 (Query Expansion)
   └── 查询改写 (Query Rewriting)

2. SearchProvider.search(改写后查询)

3. HybridFilterEngine.filter(results, query)
   ├── 计算 Title Embedding 相似度
   ├── 计算 BM25 词频得分
   ├── 混合评价加权
   └── 剔除低于阈值的结果

4. ContentFetcher.fetch(筛选后结果)
   ├── URL 内容获取
   ├── HTML 文本提取
   └── 异常处理与剔除

5. Multi-factorReranker.rerank(结果列表, query)
   ├── 计算时效性得分 (Freshness)
   ├── 计算相关性得分 (Relevance)
   ├── 查询天梯获取可信度得分 (Trust)
   ├── 综合加权排序
   └── 返回 Top 50 结果

6. FactExtractor.extract(正文内容, source_domain)
   ├── 句子分割
   ├── 事实性判断
   ├── SPO 三元组提取
   ├── NLI 标签分析
   ├── 数值/时间提取
   └── 置信度评分

7. FactBucketCluster.cluster(事实列表)
   ├── Embedding 相似度计算
   ├── 层次聚类/贪心聚类
   └── 事实桶划分

8. OrthogonalCollisionDetector.detect(事实桶)
   ├── NLI 冲突检测
   ├── SPO 冲突检测
   ├── 数值/时间冲突检测
   ├── 碰撞系数计算
   └── 低碰撞 → 可信事实 / 高碰撞 → LLM 复核

9. 输出可信事实清单
```

### 9.2 时序图

```
用户          SearchOrchestrator     各模块              LLM Client
 │                   │                   │                    │
 │  search(query)    │                   │                    │
 │──────────────────>│                   │                    │
 │                   │                   │                    │
 │                   │  rewrite(query)   │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  rewritten_queries│                    │
 │                   │                   │                    │
 │                   │  search(queries)  │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  search_results   │                    │
 │                   │                   │                    │
 │                   │  filter(results)  │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  filtered_results │                    │
 │                   │                   │                    │
 │                   │  fetch_content()  │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  content_results  │                    │
 │                   │                   │                    │
 │                   │  rerank(content)  │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  top50_results    │                    │
 │                   │                   │                    │
 │                   │  extract_facts()  │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  extracted_facts  │                    │
 │                   │                   │                    │
 │                   │  cluster_facts()  │                    │
 │                   │──────────────────>│                    │
 │                   │<──────────────────│                    │
 │                   │  fact_buckets     │                    │
 │                   │                   │                    │
 │                   │  detect_collision()│                   │
 │                   │──────────────────>│                    │
 │                   │                   │                    │
 │                   │  (if collision >= threshold)           │
 │                   │                   │    llm_review()    │
 │                   │                   │───────────────────>│
 │                   │                   │<───────────────────│
 │                   │                   │    review_result   │
 │                   │                   │                    │
 │                   │<──────────────────│                    │
 │                   │  collision_results│                    │
 │                   │                   │                    │
 │  trusted_facts   │                   │                    │
 │<──────────────────│                   │                    │
 │                   │                   │                    │
```

### 9.3 代码示例

```python
from web_search.core.orchestrator import SearchOrchestrator
from web_search.providers.factory import create_provider
from web_search.classifier.source_classifier import SourceClassifier
from web_search.resolver.fact_resolver import FactResolver
from web_search.summary.summary_generator import SummaryGenerator

# v3 完整流程示例
async def trusted_search_v3(query: str):
    # 1. 创建 v3 模块实例
    provider = create_provider("searxng", {})
    hybrid_filter = HybridFilterEngine(
        embedding_weight=0.6,
        bm25_weight=0.4,
        min_hybrid_score=0.3
    )
    content_fetcher = ContentFetcher(timeout=10.0)
    multi_reranker = MultiFactorReranker(
        config=RerankConfig(
            weights={"freshness": 0.3, "relevance": 0.4, "trust": 0.3},
            freshness_lambda=0.1,
            top_k=50
        )
    )
    fact_extractor = FactExtractor()
    fact_cluster = FactBucketCluster(similarity_threshold=0.75)
    collision_detector = OrthogonalCollisionDetector(
        config=OrthogonalCollisionConfig(threshold=0.3),
        llm_client=llm_client
    )
    trust_ladder = TrustRankLadder(storage_path="./data/trust_scores.json")

    # 2. 查询改写
    rewritten_queries = await query_rewriter.rewrite(query)

    # 3. 搜索
    all_results = []
    for q in rewritten_queries:
        results = await provider.search(q)
        all_results.extend(results)

    # 4. 混合过滤
    filtered = await hybrid_filter.filter(all_results, query)
    filtered_results = [r.result for r in filtered]

    # 5. 内容获取
    content_results = content_fetcher.fetch(filtered_results)
    successful = [r for r in content_results if r.fetch_success]

    # 6. 多因子重排
    reranked = multi_reranker.rerank(successful, query)

    # 7. 事实提取
    all_facts = []
    for item in reranked:
        source_domain = item.result.source_domain
        facts = fact_extractor.extract_sync(item.content, source_domain)
        for fact in facts:
            fact.source_domain = source_domain
            fact.trust_score = trust_ladder.get_trust_score(source_domain)
        all_facts.extend(facts)

    # 8. 事实聚类
    buckets = await fact_cluster.cluster(all_facts)

    # 9. 碰撞检测
    trusted_facts = collision_detector.get_trusted_facts(buckets)

    return trusted_facts
```

---

## 10. v2 到 v3 迁移指南

### 10.1 主要变化点

| 变化类别 | v2 实现 | v3 实现 | 迁移说明 |
|---------|---------|---------|---------|
| **初筛模块** | `LLMSourceClassifier` | `HybridFilterEngine` | 移除 LLM 调用，改用 Embedding + BM25 |
| **黑白名单** | `WhitelistChecker` / `BlacklistChecker` | `TrustRankLadder` | 静态名单 → 动态 ELO 评分 |
| **碰撞检测** | `EmbeddingSimilarity` | `OrthogonalCollisionDetector` | 单一语义相似度 → NLI/SPO/数值多维检测 |
| **重排模块** | `Reranker` | `MultiFactorReranker` | 单一相关性 → 时效+相关+可信三维 |
| **编排器** | `SearchOrchestrator` (v2) | `SearchOrchestrator` (v3) | 流程重组，新增事实处理流水线 |

### 10.2 迁移步骤

#### 步骤 1: 替换初筛模块

```python
# v2
from web_search.classifier.llm_classifier import LLMSourceClassifier
classifier = LLMSourceClassifier()
filtered = await classifier.classify(results, query)

# v3
from web_search.filter import HybridFilterEngine
filter_engine = HybridFilterEngine(
    embedding_weight=0.6,
    bm25_weight=0.4,
    min_hybrid_score=0.3
)
filtered = await filter_engine.filter(results, query)
```

#### 步骤 2: 引入内容获取器

```python
# v3 新增
from web_search.fetcher import ContentFetcher
fetcher = ContentFetcher(timeout=10.0)
content_results = fetcher.fetch(filtered_results)
successful = [r for r in content_results if r.fetch_success]
```

#### 步骤 3: 更新重排模块

```python
# v2
from web_search.reranker import Reranker
reranker = Reranker()
reranked = await reranker.rerank(results, options)

# v3
from web_search.reranker import MultiFactorReranker, RerankConfig
reranker = MultiFactorReranker(
    config=RerankConfig(
        weights={"freshness": 0.3, "relevance": 0.4, "trust": 0.3},
        freshness_lambda=0.1,
        top_k=50
    )
)
reranked = reranker.rerank(content_results, query)
```

#### 步骤 4: 集成可信度天梯

```python
# v3 新增
from web_search.trust import TrustRankLadder
ladder = TrustRankLadder(storage_path="./data/trust_scores.json")

# 新信源自动注册
for result in reranked:
    domain = result.result.source_domain
    if ladder.get_score(domain) is None:
        ladder.register_new_source(result.result.source_name, domain)
```

#### 步骤 5: 添加事实处理流水线

```python
# v3 新增
from web_search.extractor import FactExtractor
from web_search.cluster import FactBucketCluster
from web_search.collision import OrthogonalCollisionDetector

fact_extractor = FactExtractor()
fact_cluster = FactBucketCluster(similarity_threshold=0.75)
collision_detector = OrthogonalCollisionDetector(
    config=OrthogonalCollisionConfig(threshold=0.3),
    llm_client=llm_client
)

# 提取事实
all_facts = []
for item in reranked:
    facts = fact_extractor.extract_sync(item.content, item.result.source_domain)
    all_facts.extend(facts)

# 聚类
buckets = await fact_cluster.cluster(all_facts)

# 碰撞检测
trusted_facts = collision_detector.get_trusted_facts(buckets)
```

### 10.3 兼容性说明

#### 向后兼容

v3 架构的 `SearchOrchestrator` 保持与 v2 API 的兼容性：

```python
# v2 风格的调用仍然有效
orchestrator = SearchOrchestrator(provider, source_classifier)
result = orchestrator.search_with_trust(query, options)
```

#### 配置迁移

```yaml
# v3 配置文件示例: config/v3.yaml

hybrid_filter:
  embedding_weight: 0.6
  bm25_weight: 0.4
  min_hybrid_score: 0.3

content_fetcher:
  timeout: 10.0
  max_content_length: 100000
  min_text_length: 100

reranker:
  weights:
    freshness: 0.3
    relevance: 0.4
    trust: 0.3
  freshness_lambda: 0.1
  top_k: 50

trust_ladder:
  storage_path: "./data/trust_scores.json"
  use_sqlite: false
  default_initial_score: 1000

fact_extraction:
  min_confidence_threshold: 0.3
  max_facts_per_content: 50

collision_detection:
  alpha: 0.4
  beta: 0.3
  gamma: 0.3
  threshold: 0.3
```

#### 数据迁移

```python
# 将 v2 的静态黑白名单迁移到 v3 的 TrustRankLadder

# v2 名单
whitelist_domains = ["cctv.com", "xinhuanet.com", "people.com.cn"]
blacklist_domains = ["fake-news.com", "spam-site.com"]

# v3 迁移脚本
for domain in whitelist_domains:
    trust_ladder.register_new_source(
        source_name=domain,
        source_domain=domain,
        initial_score=1200  # 白名单信源初始分数较高
    )

for domain in blacklist_domains:
    trust_ladder.register_new_source(
        source_name=domain,
        source_domain=domain,
        initial_score=700  # 黑名单信源初始分数较低
    )
```

---

## 附录 A: v3 模块列表

### A.1 模块清单

| 模块路径 | 类/函数 | 功能描述 | 依赖 |
|---------|---------|---------|------|
| `filter/hybrid_filter_engine.py` | `HybridFilterEngine` | 混合过滤引擎 | `EmbeddingScorer`, `BM25Scorer` |
| `filter/embedding_scorer.py` | `EmbeddingScorer` | Embedding 相似度计算 | `EmbeddingClient` |
| `filter/bm25_scorer.py` | `BM25Scorer` | BM25 词频评分 | 无 |
| `fetcher/content_fetcher.py` | `ContentFetcher` | URL 内容获取 | `TextExtractor` |
| `fetcher/text_extractor.py` | `TextExtractor` | HTML 文本提取 | `trafilatura` / `BeautifulSoup` |
| `trust/trust_rank_ladder.py` | `TrustRankLadder` | 可信度天梯 | `ELOCalculator`, `SourceRegistry` |
| `trust/elo_calculator.py` | `ELOCalculator` | ELO 分数计算 | 无 |
| `trust/source_registry.py` | `SourceRegistry` | 信源注册表 | JSON/SQLite |
| `reranker/multi_factor_reranker.py` | `MultiFactorReranker` | 多因子重排 | `FreshnessScorer`, `TrustScorer` |
| `reranker/freshness_scorer.py` | `FreshnessScorer` | 时效性评分 | 无 |
| `reranker/trust_scorer.py` | `TrustScorer` | 可信度评分 | `TrustRankLadder` |
| `extractor/fact_extractor.py` | `FactExtractor` | 事实提取器 | `NLIAnalyzer`, `SPOExtractor`, `ValueExtractor` |
| `extractor/nli_analyzer.py` | `NLIAnalyzer` | NLI 分析 | `CrossEncoder` / `LLMClient` |
| `extractor/spo_extractor.py` | `SPOExtractor` | SPO 三元组提取 | `spaCy` / `LLMClient` |
| `extractor/value_extractor.py` | `ValueExtractor` | 数值/时间提取 | 正则表达式 |
| `cluster/fact_bucket_cluster.py` | `FactBucketCluster` | 事实桶聚类 | `EmbeddingClient`, `scipy` |
| `collision/orthogonal_detector.py` | `OrthogonalCollisionDetector` | 正交碰撞检测 | `NLICollisionDetector`, `SPOCollisionDetector`, `ValueCollisionDetector` |
| `collision/nli_collision.py` | `NLICollisionDetector` | NLI 冲突检测 | 无 |
| `collision/spo_collision.py` | `SPOCollisionDetector` | SPO 冲突检测 | 无 |
| `collision/value_collision.py` | `ValueCollisionDetector` | 数值/时间冲突检测 | 无 |

### A.2 数据模型清单

| 数据模型 | 定义位置 | 描述 |
|---------|---------|------|
| `HybridFilterResult` | `filter/hybrid_filter_engine.py` | 混合过滤结果 |
| `ContentFetchResult` | `fetcher/content_fetcher.py` | 内容获取结果 |
| `TextExtractionResult` | `fetcher/text_extractor.py` | 文本提取结果 |
| `TrustRankScore` | `trust/trust_rank_ladder.py` | 信源可信度分数 |
| `TrustLevel` | `trust/trust_rank_ladder.py` | 可信度等级枚举 |
| `ExtractedFact` | `extractor/fact_extractor.py` | 提取的事实 |
| `SPOTriple` | `extractor/spo_extractor.py` | SPO 三元组 |
| `NumericValue` | `extractor/value_extractor.py` | 数值信息 |
| `DatetimeValue` | `extractor/value_extractor.py` | 时间信息 |
| `FactBucket` | `cluster/fact_bucket_cluster.py` | 事实桶 |
| `CollisionResult` | `collision/__init__.py` | 碰撞检测结果 |
| `TrustedFact` | `collision/__init__.py` | 可信事实 |
| `NLILabel` | `collision/__init__.py` | NLI 标签枚举 |

### A.3 配置文件清单

| 配置文件 | 路径 | 用途 |
|---------|------|------|
| `embedding.yaml` | `configs/embedding.yaml.example` | Embedding 服务配置 |
| `llm.yaml` | `configs/llm.yaml.example` | LLM 服务配置 |
| `providers.yaml` | `configs/providers.yaml.example` | 搜索提供者配置 |
| `reranker.yaml` | `configs/reranker.yaml.example` | 重排模块配置 |
| `rewriter.yaml` | `configs/rewriter.yaml` | 查询改写配置 |
| `whitelist.yaml` | `configs/whitelist.yaml` | 白名单配置 |
| `blacklist.yaml` | `configs/blacklist.yaml` | 黑名单配置 |

---

## 文档版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| v1.0 | 2025-XX-XX | - | 初始版本 |
| v2.0 | 2025-XX-XX | - | v2 架构设计 |
| v3.0 | 2026-04-25 | - | v3 架构设计，基于实际实现 |

---

*本文档由系统自动生成，基于实际代码实现。*
